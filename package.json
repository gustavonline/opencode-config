{
  "name": "opencode-config",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Cross-platform OpenCode configuration with MCP toolbox",
  "scripts": {
    "test": "node tools/slack.js list-channels 2>/dev/null || echo 'Slack not configured'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.3",
    "@notionhq/client": "^5.8.0",
    "dotenv": "^17.2.3",
    "node-fetch": "^3.3.2"
  }
}
